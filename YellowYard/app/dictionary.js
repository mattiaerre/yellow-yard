import { version } from '../package.json';

const dictionary = {
  title: 'Pace converter',
  version: `v${version}`,
  description: 'Universal running pace converter.'
};

export default dictionary;
